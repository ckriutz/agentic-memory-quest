# Local-only environment variables.
# This file is gitignored. Do NOT commit real secrets.
#
# For Azure Container Apps deployment via scripts/deploy_containerapps.sh

# --- GHCR ---
# If your GHCR images are PUBLIC, you do NOT need GHCR_USERNAME/GHCR_TOKEN.
# You DO still need GHCR_OWNER (the GitHub org/user) unless you override SERVER_IMAGE/WEB_IMAGE.
GHCR_OWNER=ckriutz

# Only required for PRIVATE images:
GHCR_USERNAME=
GHCR_TOKEN=

# --- Azure target ---
AZ_SUBSCRIPTION_ID=
AZ_LOCATION=westus3
AZ_RESOURCE_GROUP=agentic-memory-quest-rg
AZ_CONTAINERAPPS_ENV=amq-aca-env

# --- Image selection ---
# Use 'latest' or the GitHub Actions run_number tag
IMAGE_TAG=latest

# --- Server required runtime settings ---
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=

# Optional server settings
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_VERSION=
QDRANT_HOST=
QDRANT_PORT=
HINDSIGHT_URL=

# --- Azure AI Foundry (agent reference) ---
# Azure AD auth via DefaultAzureCredential. Set one of the endpoint options plus agent name.
# Full endpoint format: https://<your-resource>.services.ai.azure.com/api/projects/<your-project>
AZURE_FOUNDRY_ENDPOINT=
# Or build it from parts:
AZURE_FOUNDRY_RESOURCE_NAME=
AZURE_FOUNDRY_RESOURCE_ENDPOINT=
AZURE_FOUNDRY_PROJECT=
AZURE_FOUNDRY_AGENT_NAME=
